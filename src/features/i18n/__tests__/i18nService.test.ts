// 簡單的國際化ServiceTest
describe('I18nService Basic Tests', () => {
  it('應該存在', () => {
    expect(true).toBe(true);
  });

  it('應該支持基本功能', () => {
    // 這裡只YesVerifyTest環境正常工作
    const _testValue = 'test';
    expect(testValue).toBe('test');
  });
});
