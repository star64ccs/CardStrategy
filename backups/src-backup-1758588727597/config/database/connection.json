{
  "host": "${DB_HOST}",
  "port": "${DB_PORT}",
  "database": "${DB_NAME}",
  "user": "${DB_USER}",
  "password": "${DB_PASSWORD}",
  "ssl": "${DB_SSL}",
  "pool": {
    "min": 2,
    "max": 10,
    "idleTimeoutMillis": 30000
  }
}